# Table 1 / figures from published tensors (no GPU, no PyTorch).
# For from-PDB extraction: pip install -r requirements-extract.txt
name: pmpnn-ddg
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - scikit-learn
  - matplotlib
  - pandas
