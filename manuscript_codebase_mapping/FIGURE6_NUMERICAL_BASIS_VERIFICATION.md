# Figure 6 Numerical Basis Verification

Date: 2026-05-11

Purpose: record the evidence that the manuscript Figure 6 standalone image is
the saved output of the historical incremental-feature notebook cell, and that
the plotted values come from the ten-run incremental-feature result pickle.

This note concerns the final RF result/plotting layer. It does not prove
upstream V3 pickle regeneration or direct tensor extraction.

## Interpretation Standard

For Figure 6, the scientific recovery target is the plotted data/content:

```text
S_669/Ssym total-PCC series
-> incremental A through A-H feature-combination plot
-> manuscript Figure 6 content
```

Byte identity is not the scientific standard for comparing a notebook PNG,
standalone PNG, and DOCX-embedded PNG. It is only a useful artifact-provenance
shortcut when it succeeds. If a DOCX-embedded image is not byte-identical, that
is not a scientific discrepancy by itself, because Word/DOCX embedding can
change image bytes, dimensions, or layout while preserving the plotted content.

## Core Finding

The standalone manuscript figure file:

```text
drive_evidence_copy/sajidahmedprotres_drive/MPNN_DDG_Manuscript/Feature_Combinations_MultiPlot.png
```

is byte-identical to the saved PNG output embedded in:

```text
source_repos/SajidAhmeduiu_ProteinMPNN/Sajid_Additions/Quick_Dirty_MPNN_ML_V2_VGRAPHS_V1.ipynb
```

matched notebook output:

```text
cell index: 40
execution count: 58
output index: 0
SHA256: 9cec9fcf1d3f466b8bf0f1766bc98c20152b2c617c5d872d3e3588f79143da95
dimensions: 1517x717
```

The exact byte match verifies that the manuscript-side Figure 6 image came from
that notebook output.

The final DOCX contains its own embedded copy of Figure 6:

```text
SA_CEM_9_26_2022_MJ_Issue_Adressing.docx -> word/media/image6.png
```

The DOCX paragraph/image references identify `media/image6.png` as Figure 6.
That embedded PNG is not byte-identical to the standalone image above, but that
does not count against Figure 6 recovery. The DOCX check establishes that the
final DOCX Figure 6 slot points to `media/image6.png`; content equivalence must
be judged by the plotted data and the visible figure content, not by byte
identity.

Manual visual comparison found no data-content discrepancy between the verified
standalone PNG and DOCX `media/image6.png`. Both show:

- the same `S_669` and `Ssym` series;
- the same red/green bar pairing;
- the same `A` through `A-H` incremental feature-combination labels;
- the same PCC metric;
- the same monotonic/near-plateau bar-height pattern implied by the recovered
  total-PCC values.

The observed differences are layout/rendering differences, not scientific-data
differences.

## Matched Plotting Cell

The matched plotting cell builds the two bar series from:

```text
S_669_PCC_vals = [i for i in dict_mean(list_S_669_total_PCC).values()]
Ssym_PCC_vals = [i for i in dict_mean(list_Ssym_total_PCC).values()]
```

It then labels the incremental feature combinations as:

```text
A, A-B, A-C, A-D, A-E, A-F, A-G, A-H
```

and plots:

```text
red bars:   S_669
green bars: Ssym
metric:     total PCC
```

The matched source is preserved at:

```text
code_inventory_analysis/figure6_image_verification/figure6_matched_notebook_cell_source.py.txt
```

## Pickle-Derived Plotted Series

The plotted total-PCC series reconstructed from
`list_incremental_feature_result_dict.pickle` is:

| Label | S_669 total PCC mean | Ssym total PCC mean |
| --- | --- | --- |
| `A` | `0.4716349516326452` | `0.6411381217091818` |
| `A-B` | `0.5260122556202218` | `0.7006985310624467` |
| `A-C` | `0.5772006420549597` | `0.7164446229490329` |
| `A-D` | `0.6010573659102072` | `0.7426379714006968` |
| `A-E` | `0.6373709366633076` | `0.7868663352314815` |
| `A-F` | `0.6437674388651097` | `0.802689928058127` |
| `A-G` | `0.6435247169825066` | `0.808747587496611` |
| `A-H` | `0.6439948414047258` | `0.8122281604920314` |

The generated TSV is:

```text
code_inventory_analysis/figure6_image_verification/figure6_plotted_total_pcc_series.tsv
```

## Verification Artifacts

Generated by:

```text
scripts/verify_figure6_image_basis.py
```

Output directory:

```text
code_inventory_analysis/figure6_image_verification/
```

Important files:

- `README.md`: short generated summary.
- `notebook_png_hashes.tsv`: all PNG outputs found in the checked notebooks.
- `figure6_image_hash_matches.tsv`: exact byte match between manuscript figure
  and notebook output.
- `docx_media_hashes.tsv`: final DOCX embedded-media hashes and dimensions.
- `docx_image_references.tsv`: DOCX image-reference/caption mapping showing
  that `media/image6.png` is referenced by Figure 6 paragraphs.
- `figure6_matched_notebook_cell_source.py.txt`: matched plotting-cell source.
- `figure6_plotted_total_pcc_series.tsv`: total-PCC series from the ten-run
  result pickle.

## Strict Status

Verified:

```text
list_incremental_feature_result_dict.pickle
-> dict_mean(list_S_669_total_PCC), dict_mean(list_Ssym_total_PCC)
-> Quick_Dirty_MPNN_ML_V2_VGRAPHS_V1.ipynb cell 40 plot
-> manuscript Feature_Combinations_MultiPlot.png / Figure 6 image
```

Still outside this verification:

```text
PDB/PSSM/mutation table
-> ProteinMPNN tensor extraction
-> V3 feature pickles
-> RF feature matrices
```

Those upstream stages remain separate recovery/reproduction work.
