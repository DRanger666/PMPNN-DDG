# Manuscript-path tensors/features (local umbrella)

Symlinks into per-dataset `pdb_to_features_*` pipeline directories.

Preferred artifact: `manuscript_path_features.pickle`  
See `manuscript_codebase_mapping/MANUSCRIPT_PATH_VS_HISTORICAL_V3.md`.

Drive upload helper: `scripts/upload_manuscript_path_tensors_to_drive.sh`
(requires rclone remote — not configured on this box yet).
